{
  "name": "turborepo-basic-shared",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "yarn db:start && yarn dev:apps",
    "dev:apps": "dotenv -e .env.development -- turbo run dev --parallel",
    "db:start": "docker-compose --env-file \"./.env.development\" up -d",
    "db:stop": "docker-compose --env-file \"./.env.development\" down",
    "api": "dotenv -e .env.development -- turbo run aspida",
    "test": "dotenv -e .env.test -- turbo run test",
    "test:e2e": "dotenv -e .env.test -- turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@cookie-gg/prettier-plugin-property-order": "1.0.0-commentout",
    "@types/node": "latest",
    "@typescript-eslint/eslint-plugin": "^5.25.0",
    "@typescript-eslint/parser": "^5.25.0",
    "dotenv-cli": "^5.1.0",
    "eslint": "^8.15.0",
    "eslint-config-next": "latest",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "latest",
    "turbo": "latest",
    "tsc-alias": "^1.6.7",
    "typescript": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "yarn@1.22.18"
}
